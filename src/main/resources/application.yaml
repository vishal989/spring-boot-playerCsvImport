server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/player
    username: test
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true